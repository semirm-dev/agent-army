name: Sync Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Check sync parity
        run: make check

      - name: Run tests
        run: make test
